# btrfs-balance-exclude-devid
Balance block groups that don't have a stripe on the excluded devid

See man page in the man directory.

Uses the python-btrfs library, **https://github.com/knorrie/python-btrfs**
